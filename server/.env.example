# Hikmat Quran Backend Environment Configuration
PORT=5000
HOST=0.0.0.0
NODE_ENV=production

# Comma-separated list of allowed client origins for CORS, or * for universal access
# Example: https://hikmat-quran.vercel.app,https://your-domain.com
CORS_ORIGIN=*
